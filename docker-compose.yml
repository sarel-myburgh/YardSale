services:
  yardsale:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - yardsale-data:/data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    restart: unless-stopped

volumes:
  yardsale-data:
